@begin:
    print("Step number ", ?turn());
    buy (2, ?raw_price());
    sell( ?production(?my_id), ?production_price());
    set $tomake 2;
    if (?raw(?my_id) < $tomake)
        set $tomake ?raw(?my_id());
    make($tomake);
    turn();
    goto @begin;
